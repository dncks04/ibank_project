# Prometheus 스크랩 설정 (docker compose 내부 네트워크 전용)
# 앱의 관리 포트(8081)는 호스트에 공개되지 않으며, 이 컨테이너만 내부에서 접근한다.
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: ibank
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["app:8081"]
